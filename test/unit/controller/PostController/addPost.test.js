const { addPost } = require('../../../../controller/PostController');
describe('test addPost function ', () => {
	/* -------------------------------------------------------------------------- */
	/*                                 happy path                                 */
	/* -------------------------------------------------------------------------- */

	it('should call ok function, post serializer, res.send function,  ', () => {
		
	});
});
